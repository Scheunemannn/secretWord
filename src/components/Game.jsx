import styles from "../styles/game.module.css"
export const Game = ({verifyLetter}) => {
  return (
    <div className={styles.game}>
      <h1>dasdadadas</h1>
      <button onClick={verifyLetter}>Voltar</button>
    </div>
  )
}
